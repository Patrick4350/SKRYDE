{
  "buildCommand": "npm ci --prefix backend && npm ci --prefix frontend && npx --prefix backend prisma generate && npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  
  "routes": [
    { "handle": "filesystem" },
    { "src": "^(?!/api)(.*)$", "dest": "/index.html" }
  ]
}

{
  "installCommand": "npm install --no-audit --no-fund --prefix backend && npm install --no-audit --no-fund --prefix frontend && cd backend && (npx --yes prisma migrate deploy || npx --yes prisma db push) && npx --yes prisma generate",
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "^(?!/api)(.*)$", "dest": "/index.html" }
  ]
}

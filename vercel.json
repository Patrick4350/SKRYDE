{
  "installCommand": "npm install --no-audit --no-fund --prefix backend && npm install --no-audit --no-fund --prefix frontend && cd backend && npx --yes prisma generate --schema prisma/schema.prisma",
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "^(?!/api)(.*)$", "dest": "/index.html" }
  ]
}
